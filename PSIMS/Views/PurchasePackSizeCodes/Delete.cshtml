@model PSIMS.Models.PurchaseModel.PurchasePackSizeCode
@using Microsoft.AspNet.Identity
@using IdentitySample.Models
@{
    ViewBag.Title = "Delete";
    ApplicationDbContext db = new ApplicationDbContext();

}

<div class="box box-body box-primary">

    <h3>Are you sure you want to delete this?</h3>
    <div>
        <h4>Purchase Pack Size Code</h4>
        <hr />
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.Location.LocationCode)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.Location.LocationCode)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.PackSize_Code)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.PackSize_Code)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.UserID)
            </dt>

            <dd>
                @{
                    var UN = Model.UserID;
                    var name = db.Users.Where(u => u.Id == UN).FirstOrDefault().UserName ?? "n/a";
                    @name

                }
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.CreatedOn)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.CreatedOn)
            </dd>

        </dl>

        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-warning btn-flat" />
            @Html.ActionLink("Back to List", "Index", null, new { @class = "btn btn-primary btn bg-olive btn-flat" })
        </div>
        }
    </div>
</div>